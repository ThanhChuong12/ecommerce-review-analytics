\section{Kết quả thực nghiệm}

\subsection{Đánh giá Mô hình Cảm xúc (PhoBERT \& Baseline)}
% Ghi chú: Chứa Learning Curves, Accuracy, Precision, Recall, Macro-F1 và Confusion Matrix.

\subsection{Báo cáo Đánh giá Phát hiện Spam/Seeding}
% Ghi chú (MỚI THÊM):
% - Dựa vào hàm `print_spam_report` trong code để vẽ Bảng thống kê.
% - Trình bày tổng số review, tỷ lệ % bắt được bằng Rule-based, tỷ lệ % bắt được bằng IForest, và Tổng số Spam bắt được sau khi hợp nhất (Union).
% - Đưa ra bảng Breakdown Top 5 Rule bị vi phạm nhiều nhất (Ví dụ: word_repetition, ai_template).

\subsection{Đánh giá Mô hình Thị giác Máy tính (ResNet50 \& CLIP)}
% Ghi chú (MỚI THÊM):
% - ResNet50 (Phân loại hỏng hóc): 
%   + Chèn biểu đồ quá trình học (Learning Curves: Loss & Accuracy) của quá trình Transfer Learning.
%   + Trình bày Ma trận nhầm lẫn (Confusion Matrix) phân loại ảnh: Nguyên vẹn (Intact) vs. Móp méo/Hư hỏng (Damaged).
%   + Các chỉ số định lượng: Precision, Recall, F1-Score cho từng lớp ảnh.
% - CLIP (Phát hiện ảnh rác/Zero-shot):
%   + Báo cáo độ chính xác của CLIP trên một tập Test nhỏ (Do CLIP chạy Zero-shot nên không có biểu đồ huấn luyện).
%   + Đưa ra ví dụ tỷ lệ % ảnh rác/ảnh không liên quan (irrelevant) mà CLIP đã lọc được.

\clearpage
